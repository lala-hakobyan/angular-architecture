import {DropdownValidator} from './dropdown-validator';


describe('DropdownValidator', () => {
  it('should create an instance', () => {
    const directive = new DropdownValidator();
    expect(directive).toBeTruthy();
  });
});
