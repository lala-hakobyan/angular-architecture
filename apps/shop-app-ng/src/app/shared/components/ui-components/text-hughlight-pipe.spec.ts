import { TextHughlightPipe } from './text-hughlight-pipe';

describe('TextHughlightPipe', () => {
  it('create an instance', () => {
    const pipe = new TextHughlightPipe();
    expect(pipe).toBeTruthy();
  });
});
